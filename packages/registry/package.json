{
  "name": "@baton/registry",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "BATON — explicit state transitions, equivocation detection, and replay protection on top of packages/align's write gate and packages/sign's ed25519 attestations. Zero third-party dependencies.",
  "license": "MIT",
  "main": "transitions.js",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
