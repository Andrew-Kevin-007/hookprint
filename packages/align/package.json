{
  "name": "@baton/align",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "BATON — deterministic paraphrase realignment and four-class claim diff. Zero dependencies, no network, no LLM.",
  "license": "MIT",
  "main": "index.js",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
