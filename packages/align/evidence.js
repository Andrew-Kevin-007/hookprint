/**
 * evidence.js — sha256 over source file bytes, span -> quote resolution, and
 * pointer construction, so every claim and every delta is clickable.
 *
 * Owns: file hashing and pointers. Depends on: node:crypto, node:fs, contract.
 */

export {};
